# Vercel Python serverless runtime looks for api/requirements.txt
# (function-scoped) when the project's primary framework is Next.js.
# Keep this in sync with the root requirements.txt.

pydantic>=2.7
pydantic-settings>=2.4
sqlmodel>=0.0.22
psycopg[binary]>=3.2
httpx>=0.27
structlog>=24.4
pyyaml>=6.0
python-dateutil>=2.9
tenacity>=9.0
python-telegram-bot[webhooks]>=21.6
