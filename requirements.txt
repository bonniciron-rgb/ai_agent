# Runtime dependencies for the Vercel Telegram webhook function.
# Listed explicitly (not via -e .) because Vercel's Python environment is
# managed by uv and blocks editable installs.

pydantic>=2.7
pydantic-settings>=2.4
sqlmodel>=0.0.22
psycopg[binary]>=3.2
httpx>=0.27
structlog>=24.4
pyyaml>=6.0
python-dateutil>=2.9
tenacity>=9.0
python-telegram-bot[webhooks]>=21.6
